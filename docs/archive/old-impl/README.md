# OUT OF CONTEXT — DO NOT READ

This directory contains the archived previous implementation of the sequencer
project. It is **explicitly out of scope** for any future work.

Future AI agents and contributors:

- Do **not** read these files when planning, designing, or implementing new
  features. They reflect a different architecture, different constraints, and
  trade-offs that no longer apply.
- Do **not** use them as a source of truth for hardware behaviour, Grid API
  usage, or memory budgets. Authoritative hardware references live in
  `docs/archive/GRID_CONTROLS.md`, `docs/archive/LIB-2-HW-MAP.md`, and
  `docs/archive/GRID_HARDWARE_API.md`.
- Do **not** copy code from here into the new tree. The greenfield rewrite
  intentionally starts clean.

This directory exists only as historical record. Treat it as if it were a
separate project that ended.
